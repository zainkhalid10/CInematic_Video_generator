# === Core ===
python-dotenv>=1.0.0
pydantic>=2.5.0

# === LangChain / LangGraph (spec §3) ===
langchain>=0.2.0
langchain-ollama>=0.1.0
langchain-anthropic>=0.3.0
langgraph>=0.1.0

# === TTS (spec §6) ===
# Coqui: multi-speaker offline VITS (recommended for per-character voices)
TTS>=0.22.0
soundfile>=0.12.0
# Bark (optional): pip install suno-bark — set TTS_ENGINE=bark
# edge-tts: free online Microsoft voices — set TTS_ENGINE=edge-tts if Coqui install fails
edge-tts>=6.1.0
pydub>=0.25.1

# === Image & Video ===
httpx>=0.27.0
requests>=2.31.0
moviepy>=1.0.3
Pillow>=10.0.0
opencv-python>=4.9.0
numpy>=1.26.0

# === Web (Phase 4) ===
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
